export interface LocationToPersistence {
  location_id: string,
  street: string,
  city: string,
  state: string,
  postcode: number,
  latitude: string,
  longitude: string,
  off: string,
  description: string,
}
